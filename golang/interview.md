# 一、Golang基础知识

## 1.函数与方法

- Go 支持函数作为“一等公民”吗？如何将函数作为参数传递？请写一个简单的例子。

## 2. 并发与Channel

- 什么是 goroutine？它和操作系统线程有何区别？
- 无缓冲 channel 和有缓冲 channel 的行为差异是什么？什么情况下会导致 goroutine 阻塞？

## 3. 接口（Interface）与反射（Reflection）

- Go 的接口是隐式实现的，这带来了哪些好处和潜在的设计陷阱？
- 您在简历中提到了“幂等性”设计。假设有一个通用的幂等执行器，它需要接收任意类型的请求并调用其对应的处理方法。在不使用代码生成的情况下，您会如何利用 Go 的接口和反射来设计这个执行器？请简述思路。

## 4. Map 的并发安全

- Go 的原生 `map` 为什么不是并发安全的？在您的“流量计费模块”中，需要聚合每5分钟的流量数据。如果多个 `goroutine` 同时向同一个 `map` 写入不同链路的数据，可能会发生什么问题？您会采用哪些策略来保证其线程安全？（例如 `sync.Map`, `RWMutex` 等），并比较它们的优劣。

## 5. `context.Context` 

> 主要用途是什么？请列举三个常见使用场景。

- 直接传递context.Context就可以了么？给一个使用场景

## 6. 包与初始化

- `init()` 函数的作用是什么？一个包中可以有多个 `init()` 函数吗？它们的执行顺序是怎样的？
- `go mod` 是什么？`go.mod` 文件中 `module`、`require`、`replace` 字段分别表示什么？

## 7. defer

`defer` 的执行时机是什么？多个 `defer` 的调用顺序是怎样的

# 二、算法题目

## 1. 反转链表

> func reverseList(head *ListNode) *ListNode

## 2. 判断是否为回文

> 实现一个函数 `isPalindrome(s string) bool`，判断一个字符串是否是回文

## 3. 合并两个有序数组

> 给你两个按非递减顺序排列的整数数组 `nums1` 和 `nums2`，另有两个整数 `m` 和 `n`，分别表示 `nums1` 和 `nums2` 中的有效元素数目。
